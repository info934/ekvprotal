import React from 'react';

// Map component has been removed from the project.
// Keeping the file structure to prevent import errors in case of lingering references,
// but rendering nothing.
const MapApp = () => {
  return null;
};

export default MapApp;