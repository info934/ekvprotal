import React, { useEffect, useMemo, useState } from 'react'; import { PackagePlus, Search } from 'lucide-react'; import { Badge } from '@/components/ui/badge'; import { Button } from '@/components/ui/button'; import { Checkbox } from '@/components/ui/checkbox'; import {   Dialog,   DialogContent,   DialogFooter,   DialogHeader,   DialogTitle, } from '@/components/ui/dialog'; import { Input } from '@/components/ui/input'; import { Label } from '@/components/ui/label'; import {   Select,   SelectContent,   SelectItem,   SelectTrigger,   SelectValue, } from '@/components/ui/select'; import {   Table,   TableBody,   TableCell,   TableHead,   TableHeader,   TableRow, } from '@/components/ui/table';  const formatCurrency = (value) => new Intl.NumberFormat('cs-CZ', {   style: 'currency',   currency: 'CZK',   maximumFractionDigits: 0, }).format(Number(value || 0));  const productKey = (product) => product.id || product.code || product.sku || product.name;  const uniqueSortedValues = (products, field) => (   Array.from(new Set(products.map((product) => product[field]).filter(Boolean)))     .sort((a, b) => String(a).localeCompare(String(b), 'cs')) );  const CrmProductPickerDialog = ({   open,   onOpenChange,   products = [],   loading = false,   onApply, }) => {   const [query, setQuery] = useState('');   const [category, setCategory] = useState('all');   const [productType, setProductType] = useState('all');   const [status, setStatus] = useState('active');   const [selectedKeys, setSelectedKeys] = useState({});    useEffect(() => {     if (!open) {       setQuery('');       setSelectedKeys({});     }   }, [open]);    const categories = useMemo(() => uniqueSortedValues(products, 'category'), [products]);   const productTypes = useMemo(() => uniqueSortedValues(products, 'product_type'), [products]);    const filteredProducts = useMemo(() => {     const needle = query.trim().toLowerCase();     return products.filter((product) => {       const isArchived = product.is_active === false;       if (status === 'active' && isArchived) return false;       if (status === 'archived' && !isArchived) return false;       if (category !== 'all' && product.category !== category) return false;       if (productType !== 'all' && product.product_type !== productType) return false;       if (!needle) return true;       return [product.code, product.sku, product.name, product.description, product.category, product.product_type]         .filter(Boolean)         .some((value) => String(value).toLowerCase().includes(needle));     });   }, [category, productType, products, query, status]);    const selectedProducts = useMemo(() => (     products.filter((product) => selectedKeys[productKey(product)])   ), [products, selectedKeys]);    const toggleProduct = (product, checked) => {     const key = productKey(product);     setSelectedKeys((current) => {       const next = { ...current };       if (checked) next[key] = true;       else delete next[key];       return next;     });   };    const handleApply = () => {     onApply?.(selectedProducts);     setSelectedKeys({});     onOpenChange?.(false);   };    return (     <Dialog open={open} onOpenChange={onOpenChange}>       <DialogContent className="max-h-[88vh] max-w-6xl overflow-hidden p-0">         <DialogHeader className="border-b px-5 py-4">           <DialogTitle className="flex items-center gap-2 text-base">             <PackagePlus className="h-4 w-4 text-primary" />             Produktový katalog           </DialogTitle>         </DialogHeader>          <div className="space-y-3 p-4">           <div className="grid gap-2 md:grid-cols-[minmax(220px,1fr)_180px_180px_160px]">             <div className="relative">               <Search className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-muted-foreground" />               <Input                 value={query}                 onChange={(event) => setQuery(event.target.value)}                 placeholder="Hledat kód, název, popis..."                 className="h-9 pl-9 text-sm"               />             </div>             <Select value={category} onValueChange={setCategory}>               <SelectTrigger className="h-9 text-sm">                 <SelectValue placeholder="Kategorie" />               </SelectTrigger>               <SelectContent>                 <SelectItem value="all">Všechny kategorie</SelectItem>                 {categories.map((item) => (                   <SelectItem key={item} value={item}>{item}</SelectItem>                 ))}               </SelectContent>             </Select>             <Select value={productType} onValueChange={setProductType}>               <SelectTrigger className="h-9 text-sm">                 <SelectValue placeholder="Produktová řada" />               </SelectTrigger>               <SelectContent>                 <SelectItem value="all">Všechny řady</SelectItem>                 {productTypes.map((item) => (                   <SelectItem key={item} value={item}>{item}</SelectItem>                 ))}               </SelectContent>             </Select>             <Select value={status} onValueChange={setStatus}>               <SelectTrigger className="h-9 text-sm">                 <SelectValue placeholder="Stav" />               </SelectTrigger>               <SelectContent>                 <SelectItem value="active">Aktivní</SelectItem>                 <SelectItem value="archived">Archivované</SelectItem>                 <SelectItem value="all">Vše</SelectItem>               </SelectContent>             </Select>           </div>            <div className="overflow-hidden rounded-md border bg-white">             <div className="max-h-[48vh] overflow-auto">               <Table>                 <TableHeader className="sticky top-0 z-10 bg-slate-50">                   <TableRow>                     <TableHead className="w-10" />                     <TableHead className="min-w-[120px]">Kód</TableHead>                     <TableHead className="min-w-[260px]">Název produktu</TableHead>                     <TableHead className="min-w-[130px] text-right">Standardní cena</TableHead>                     <TableHead className="min-w-[90px] text-right">DPH</TableHead>                     <TableHead className="min-w-[150px]">Kategorie / řada</TableHead>                     <TableHead className="min-w-[280px]">Popis</TableHead>                   </TableRow>                 </TableHeader>                 <TableBody>                   {loading ? (                     <TableRow>                       <TableCell colSpan={7} className="h-24 text-center text-muted-foreground">Načítám katalog...</TableCell>                     </TableRow>                   ) : filteredProducts.length === 0 ? (                     <TableRow>                       <TableCell colSpan={7} className="h-24 text-center text-muted-foreground">Žádný produkt neodpovídá filtru.</TableCell>                     </TableRow>                   ) : filteredProducts.map((product) => {                     const key = productKey(product);                     const checked = Boolean(selectedKeys[key]);                     return (                       <TableRow key={key} className="cursor-pointer hover:bg-blue-50/40" onClick={() => toggleProduct(product, !checked)}>                         <TableCell onClick={(event) => event.stopPropagation()}>                           <Checkbox checked={checked} onCheckedChange={(value) => toggleProduct(product, Boolean(value))} />                         </TableCell>                         <TableCell className="font-medium text-slate-800">{product.code || product.sku || '-'}</TableCell>                         <TableCell>                           <div className="font-medium text-slate-950">{product.name || 'Produkt bez názvu'}</div>                           {product.sku && <div className="text-xs text-muted-foreground">{product.sku}</div>}                         </TableCell>                         <TableCell className="text-right font-semibold">{formatCurrency(product.default_unit_price)}</TableCell>                         <TableCell className="text-right">{Number(product.default_vat_rate ?? 21)} %</TableCell>                         <TableCell>                           <div className="flex flex-wrap gap-1">                             {product.category && <Badge variant="secondary">{product.category}</Badge>}                             {product.product_type && <Badge variant="outline">{product.product_type}</Badge>}                           </div>                         </TableCell>                         <TableCell className="max-w-[320px] truncate text-muted-foreground">{product.description || '-'}</TableCell>                       </TableRow>                     );                   })}                 </TableBody>               </Table>             </div>           </div>            <div className="rounded-md border bg-slate-50 p-3">             <Label className="text-xs uppercase tracking-wide text-slate-500">Vybrané produkty</Label>             {selectedProducts.length === 0 ? (               <p className="mt-2 text-sm text-muted-foreground">Zatím není vybrán žádný produkt.</p>             ) : (               <div className="mt-2 flex flex-wrap gap-2">                 {selectedProducts.map((product) => (                   <Badge key={productKey(product)} variant="secondary" className="gap-1">                     {product.code || product.sku || '-'} · {product.name}                   </Badge>                 ))}               </div>             )}           </div>         </div>          <DialogFooter className="border-t bg-slate-50 px-5 py-3">           <Button type="button" variant="outline" onClick={() => onOpenChange?.(false)}>Zrušit</Button>           <Button type="button" onClick={handleApply} disabled={selectedProducts.length === 0}>             Vložit vybrané ({selectedProducts.length})           </Button>         </DialogFooter>       </DialogContent>     </Dialog>   ); };  export default CrmProductPickerDialog;